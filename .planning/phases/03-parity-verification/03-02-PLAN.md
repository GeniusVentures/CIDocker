---
phase: 03-parity-verification
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - almalinux-8/verify-parity.sh
autonomous: true
requirements: [PAR-01, PAR-02]
user_setup: []

must_haves:
  truths:
    - "Running `bash almalinux-8/verify-parity.sh` exits 0 only when the stated-major matrix (rustc 1.87.0, node 24, java 25, mold 2.42.0) matches on both images"
    - "The C++ and Rust fixture build and run on both images with identical stdout (cpp-ok 15 / rust-ok 15)"
    - "The GTK probe compiles against EL8 gtk3-devel and both images' gtk+-3.0 versions are reported"
    - "Clang 11->17 is documented as accepted drift and ruby 2.7->3.1 is probed with `ruby -e` on both images"
  artifacts:
    - path: almalinux-8/verify-parity.sh
      provides: "side-by-side parity harness (D-01)"
      contains: "docker info"
    - path: .planning/phases/03-parity-verification/03-PARITY-REPORT.md
      provides: "durable drift/parity evidence (generated at run-time)"
      contains: "PARITY"
  key_links:
    - from: almalinux-8/verify-parity.sh
      to: "cidocker-almalinux-8:parity (local build) and ghcr.io/geniusventures/debian-bullseye:latest (pull)"
      via: "docker build / docker pull"
      pattern: "docker (build|pull)"
    - from: almalinux-8/verify-parity.sh
      to: "almalinux-8/fixture"
      via: "docker run -v .../fixture:/src:ro"
      pattern: "fixture:/src:ro"
    - from: almalinux-8/verify-parity.sh
      to: "gtk3-devel"
      via: "pkg-config --cflags --libs gtk+-3.0"
      pattern: "gtk\\+-3.0"
---

## Phase Goal

**As a** CI maintainer, **I want to** verify the AlmaLinux 8 image behaves identically to `debian-bullseye`, **so that** consuming builds keep working after the bullseye EOL switch.

<objective>
Create the versioned parity harness `almalinux-8/verify-parity.sh` (D-01) that runs the two images side-by-side and emits evidence for all four Phase 3 success criteria: Stage 1 version matrix (PAR-01), Stage 2 behavioral fixture diff (PAR-01), Stage 3 GTK compile probe + monorepo grep (PAR-02), and the clang/Ruby drift record (D-08/D-09).

Purpose: Produce the single manual-run script that is this phase's sign-off evidence, without modifying the Dockerfiles or `.github/workflows/ci.yml` (D-02).
Output: `almalinux-8/verify-parity.sh` plus a run-time-generated `.planning/phases/03-parity-verification/03-PARITY-REPORT.md`.
</objective>

<execution_context>
@.planning/phases/03-parity-verification/03-RESEARCH.md
@.planning/phases/03-parity-verification/03-CONTEXT.md
@.planning/phases/03-parity-verification/03-PATTERNS.md
</execution_context>

<context>
@debian-bullseye/Dockerfile
@almalinux-8/Dockerfile
@.github/workflows/ci.yml
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
# Fixture files created in plan 03-01 (mounted read-only by the harness):
@almalinux-8/fixture/cpp/main.cpp
@almalinux-8/fixture/rust/Cargo.toml
@almalinux-8/fixture/rust/src/main.rs
@almalinux-8/fixture/gtk_probe.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-parity.sh scaffold + Stage 1 version matrix (PAR-01)</name>
  <files>almalinux-8/verify-parity.sh</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\verify-parity.sh (the file being created)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-RESEARCH.md ("Code Examples" -> verify-parity.sh skeleton; "Standard Stack"; "Common Pitfalls" 1-3)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-CONTEXT.md (D-01, D-02, D-03)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-PATTERNS.md ("Shared Patterns")
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (toolchain pins + verification probes)
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (step 10 verification chain, lines 215-229)
    - W:\gnus\CIDocker\.github\workflows\ci.yml (ghcr tag `ghcr.io/geniusventures/debian-bullseye:latest`)
  </read_first>
  <action>
    Create `almalinux-8/verify-parity.sh` as a bash script (shebang `#!/usr/bin/env bash`) with `set -euo pipefail` and a leading comment block stating it is run MANUALLY per D-01 and that `ci.yml` is NOT modified (D-02). Start with `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`. Define env-var-overridable image defaults: `ALMA_IMAGE="${ALMA_IMAGE:-cidocker-almalinux-8:parity}"` and `BULLSEYE_IMAGE="${BULLSEYE_IMAGE:-ghcr.io/geniusventures/debian-bullseye:latest}"`. Add a pre-flight `docker info >/dev/null 2>&1 || { echo "FAIL: Docker daemon is not running..." >&2; exit 1; }` (Pitfall 3). Build the Alma image with `docker build -t "$ALMA_IMAGE" -f "$SCRIPT_DIR/Dockerfile" "$SCRIPT_DIR"` and pull the frozen bullseye with `docker pull "$BULLSEYE_IMAGE"`. Implement a `matrix()` shell function that runs `docker run --rm "$img" bash -c '...'` and prints one `key=value` line per tool: `rustc=$(rustc --version | awk '{print $2}')`, `node=$(node --version | sed 's/^v//' | cut -d. -f1)`, `java=$(java -version 2>&1 | sed -n 's/^openjdk version \"\([0-9]*\).*/\1/p')`, `mold=$(ld --version | awk '{print $2}')`, plus report-only lines `clang`, `cmake`, `ruby`, `gtk` (from `pkg-config --modversion gtk+-3.0`), `glibc` (from `ldd --version | head -1`). Capture the matrix for BOTH images to temp files, then hard-gate the four stated majors (D-03): for each of `rustc`, `node`, `java`, `mold`, compare the two files' values and `exit 1` printing `FAIL: <field> mismatch — bullseye=<b> almalinux=<a>` on any difference. On equality print `PASS: stated-major matrix (rustc/node/java/mold) matches`. Do NOT gate clang/cmake/ruby/gtk/glibc — those are report-only (D-08/D-09, Pitfall list in RESEARCH.md).
  </action>
  <verify>
    <automated>bash -n almalinux-8/verify-parity.sh && grep -q 'set -euo pipefail' almalinux-8/verify-parity.sh && grep -q 'docker info' almalinux-8/verify-parity.sh && grep -q 'matrix()' almalinux-8/verify-parity.sh && grep -q 'rustc' almalinux-8/verify-parity.sh && grep -q 'pkg-config --modversion gtk+-3.0' almalinux-8/verify-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `almalinux-8/verify-parity.sh` exists and `bash -n` reports no syntax errors.
    - It contains `set -euo pipefail`, the `docker info` pre-flight guard, and the `matrix()` function.
    - It contains image defaults `cidocker-almalinux-8:parity` and `ghcr.io/geniusventures/debian-bullseye:latest`.
    - It hard-gates exactly the four fields `rustc`, `node`, `java`, `mold` and exits non-zero on mismatch (D-03 bar).
    - It reports but does NOT gate `clang`, `cmake`, `ruby`, `gtk`, `glibc`.
  </acceptance_criteria>
  <done>The script scaffold, image acquisition, and Stage 1 stated-major version-matrix comparison (PAR-01, D-03) are in place and syntax-valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add Stage 2 behavioral fixture diff + Stage 3 drift evidence (PAR-01/PAR-02)</name>
  <files>almalinux-8/verify-parity.sh</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\verify-parity.sh (the file being edited — append after Stage 1)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-RESEARCH.md ("Code Examples" -> run_fixture and Stage 3; "Common Pitfalls" 4-6; "GTK Gap Check")
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-CONTEXT.md (D-05, D-06, D-07, D-08, D-09)
    - W:\gnus\CIDocker\almalinux-8\fixture\cpp\main.cpp (created in 03-01)
    - W:\gnus\CIDocker\almalinux-8\fixture\rust\src\main.rs (created in 03-01)
    - W:\gnus\CIDocker\almalinux-8\fixture\gtk_probe.c (created in 03-01)
    - W:\gnus\CIDocker\.github\workflows\ci.yml (read-only; do NOT modify)
  </read_first>
  <action>
    Append two stages to `almalinux-8/verify-parity.sh` after the Stage 1 hard gate. Stage 2 — behavioral fixture (D-05/D-06): implement a `run_fixture()` function that runs `docker run --rm -v "$SCRIPT_DIR/fixture:/src:ro" -w /src "$img" bash -eu -c '...'` which (1) asserts `ld --version | grep -q mold` (mold is default ld), (2) builds the C++ fixture with plain `clang++ -std=c++17 -O2 cpp/main.cpp -o /tmp/cpp_main` and runs it (NO `-fuse-ld=mold` — bullseye clang 11 lacks it), (3) runs the Rust fixture from `rust/` with `CARGO_TARGET_DIR=/tmp/cargo_target cargo run --quiet --release` (read-only mount + /tmp target dir, Pitfall 4), (4) runs the Node probe `node -e "console.log(\"node-ok\")"` and the Java probe (`javac` then `java` of a small `/tmp/Probe.java` printing `java-ok`) as nice-to-have D-05 probes, (5) compiles+runs the GTK probe with `cc gtk_probe.c -o /tmp/gtk_probe $(pkg-config --cflags --libs gtk+-3.0) && /tmp/gtk_probe && echo "gtk-ok"`. Capture `run_fixture` stdout for BOTH images to temp files and `diff -u` them; on any difference `exit 1` printing `FAIL: behavioral fixture output differs`; else print `PASS: behavioral fixture output identical`. Stage 3 — drift evidence: (a) GTK: print `pkg-config --modversion gtk+-3.0` for both images, and on the host grep the consuming monorepo at `${MONOREPO:-W:/gnus/GeniusNetwork}` for `#include <gtk/gtk.h>` in source extensions (`*.h *.hpp *.cpp *.cc *.c *.cxx *.rs *.m *.mm`) while excluding `thirdparty/`, `.git/`, `.dart_tool/`, `ephemeral/`, `.plugin_symlinks/`, and `flutter/`, printing matches and a verdict that any remaining hit is the out-of-scope Flutter runner template (report-only, not gated — D-07, Pitfall 6); (b) Ruby: print `ruby --version` and run `ruby -e 'puts "ruby-ok #{RUBY_VERSION}"'` on both images, then print `NOTE: ruby 2.7 (bullseye) -> 3.1 (EL8) is documented, accepted drift (D-09)`; (c) Clang: print `clang --version` head for both images and `NOTE: clang 11 -> 17 is documented, accepted drift (D-08)` with no active diffing. Finish by wrapping the three stages' combined stdout with `tee "$REPORT"` where `REPORT="${REPO_ROOT}/.planning/phases/03-parity-verification/03-PARITY-REPORT.md"` and `REPO_ROOT="$(git -C "$SCRIPT_DIR" rev-parse --show-toplevel 2>/dev/null || echo "$SCRIPT_DIR/..")"`, then print `ALL PARITY CHECKS PASSED` as the final line.
  </action>
  <verify>
    <automated>bash -n almalinux-8/verify-parity.sh && grep -q 'run_fixture()' almalinux-8/verify-parity.sh && grep -q 'fixture:/src:ro' almalinux-8/verify-parity.sh && grep -q 'CARGO_TARGET_DIR=/tmp/cargo_target' almalinux-8/verify-parity.sh && grep -q 'diff -u' almalinux-8/verify-parity.sh && grep -q 'gtk_probe.c' almalinux-8/verify-parity.sh && grep -q 'ruby -e' almalinux-8/verify-parity.sh && grep -q 'documented, accepted drift' almalinux-8/verify-parity.sh && grep -q '03-PARITY-REPORT.md' almalinux-8/verify-parity.sh && grep -q 'ALL PARITY CHECKS PASSED' almalinux-8/verify-parity.sh && ! grep -q 'fuse-ld' almalinux-8/verify-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n almalinux-8/verify-parity.sh` reports no syntax errors after appending.
    - Script contains `run_fixture()`, the read-only mount `fixture:/src:ro`, and `CARGO_TARGET_DIR=/tmp/cargo_target`.
    - Script `diff -u`s the two images' fixture stdout and exits non-zero on difference (D-06).
    - Script contains the GTK compile probe command and the host monorepo grep with the out-of-scope exclusions (D-07).
    - Script contains `ruby -e` on both images and the `documented, accepted drift` notes for ruby (D-09) and clang (D-08).
    - Script contains `ALL PARITY CHECKS PASSED` as the success marker and writes `03-PARITY-REPORT.md` via `tee`.
    - Script contains NO `-fuse-ld=mold` reference (Pitfall 1).
  </acceptance_criteria>
  <done>The full three-stage harness is complete: it builds/runs the fixture on both images, diffs behavioral output, emits GTK/clang/Ruby drift evidence, and writes a durable parity report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell -> docker daemon | The script orchestrates `docker build/pull/run`; image tags are the external input. |
| host shell -> ghcr.io | `docker pull` of the frozen bullseye image crosses the registry boundary. |
| host shell -> consuming monorepo | The GTK grep reads `W:/gnus/GeniusNetwork` on the host (read-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `docker pull ghcr.io/geniusventures/debian-bullseye:latest` | mitigate | Pull from the project's own first-party registry (pushed by this repo's `ci.yml`); document in the script header that an operator may pin `@sha256:<digest>` for stricter integrity. |
| T-03-02 | Tampering | env-var overrides `ALMA_IMAGE`/`BULLSEYE_IMAGE`/`MONOREPO` | mitigate | All expansions are double-quoted; overrides documented as trusted operator inputs only (no untrusted user input path). |
| T-03-03 | Information Disclosure | fixture build/run in container | mitigate | Fixture mounted read-only (`:ro`), std-only Rust crate (no network), outputs only fixed strings; no secrets or env passthrough. |
| T-03-04 | Elevation of Privilege | script execution | accept | No `sudo` in the script; runs the unprivileged Docker CLI. |
| T-03-05 | Tampering | `docker build` of `almalinux-8/Dockerfile` | mitigate | Builds the committed repo copy of the Dockerfile (verified in Phases 1-2); the script does not fetch a Dockerfile from the network. |
| T-03-SC | Tampering | package installs | accept | No npm/pip/crates/RPM packages installed this phase; Package Legitimacy Audit is N/A (RESEARCH.md). |
</threat_model>

<verification>
- `bash -n almalinux-8/verify-parity.sh` passes (syntax-valid on both tasks).
- Content gates from both tasks' `<verify>` pass (matrix function, pre-flight, run_fixture, GTK probe, ruby probe, drift notes, report tee, no `-fuse-ld`).
- `ci.yml`, `debian-bullseye/Dockerfile`, and `almalinux-8/Dockerfile` remain unmodified (D-02 / phase boundary) — confirm with `git status --short` showing only the two fixture/script paths as new.
- Manual sign-off (per D-01): `bash almalinux-8/verify-parity.sh` (Git Bash or WSL) must print `PASS: stated-major matrix...`, `PASS: behavioral fixture output identical`, the GTK/clang/Ruby drift evidence, and `ALL PARITY CHECKS PASSED`.
</verification>

<success_criteria>
- `almalinux-8/verify-parity.sh` is the only new file in this plan and is runnable via `bash almalinux-8/verify-parity.sh`.
- Stage 1 exits non-zero if the four stated majors (rustc 1.87.0, node 24, java 25, mold 2.42.0) differ across the two images (PAR-01 / D-03).
- Stage 2 builds and runs the C++ + Rust fixture on both images and exits non-zero if the stdout `diff` is non-empty (PAR-01 / D-06).
- Stage 3 compiles the GTK probe against EL8 `gtk3-devel`, greps the monorepo, and records clang 11->17 / ruby 2.7->3.1 as accepted drift (PAR-02 / D-07/D-08/D-09).
- A durable `03-PARITY-REPORT.md` is generated when the script is run, capturing all evidence.
</success_criteria>

<output>
Create `.planning/phases/03-parity-verification/03-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| File | Symbol(s) | Purpose |
|------|-----------|---------|
| `almalinux-8/verify-parity.sh` | `matrix()`, `run_fixture()`, main body | Side-by-side parity harness (D-01) |
| `.planning/phases/03-parity-verification/03-PARITY-REPORT.md` | (generated at run-time) | Durable parity/drift evidence |
