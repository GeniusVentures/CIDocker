---
phase: 03-parity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - almalinux-8/fixture/cpp/main.cpp
  - almalinux-8/fixture/rust/Cargo.toml
  - almalinux-8/fixture/rust/src/main.rs
  - almalinux-8/fixture/gtk_probe.c
autonomous: true
requirements: [PAR-01, PAR-02]
user_setup: []

must_haves:
  truths:
    - "The fixture directory contains a C++ source, a std-only Rust crate, and a display-free GTK compile probe"
    - "The C++ and Rust fixtures emit the fixed strings cpp-ok 15 and rust-ok 15 (no embedded version strings)"
    - "The C++ fixture is compiled with plain clang++ (no -fuse-ld=mold) so bullseye clang 11 can build it"
    - "The GTK probe compiles against gtk3-devel and never calls gtk_init (display-free)"
  artifacts:
    - path: almalinux-8/fixture/cpp/main.cpp
      provides: "clang+mold behavioral build proof (PAR-01)"
      contains: "cpp-ok"
    - path: almalinux-8/fixture/rust/Cargo.toml
      provides: "std-only crate manifest (no [dependencies])"
      contains: "[package]"
    - path: almalinux-8/fixture/rust/src/main.rs
      provides: "Rust behavioral build proof (PAR-01)"
      contains: "rust-ok"
    - path: almalinux-8/fixture/gtk_probe.c
      provides: "GTK header/lib resolution probe (PAR-02)"
      contains: "#include <gtk/gtk.h>"
  key_links:
    - from: almalinux-8/fixture/cpp/main.cpp
      to: "clang++ -> default-ld shim -> mold"
      via: "compiled in-image by verify-parity.sh"
      pattern: "clang\\+\\+"
    - from: almalinux-8/fixture/gtk_probe.c
      to: "gtk3-devel headers/libs"
      via: "pkg-config --cflags --libs gtk+-3.0"
      pattern: "gtk_get_major_version"
---

## Phase Goal

**As a** CI maintainer, **I want to** verify the AlmaLinux 8 image behaves identically to `debian-bullseye`, **so that** consuming builds keep working after the bullseye EOL switch.

<objective>
Create the minimal in-repo build fixture that the parity harness compiles and runs on both images (D-04, D-05): a C++ source proving clang + mold, a std-only Rust crate, and a display-free GTK compile probe (D-07 / PAR-02).

Purpose: Provide the reproducible sample-build inputs that make PAR-01 (toolchain behavioral parity) and PAR-02 (GTK gap non-blocking) verifiable without building the real SuperGenius/SGProcessingManager projects.
Output: Four new source files under `almalinux-8/fixture/`.
</objective>

<execution_context>
@.planning/phases/03-parity-verification/03-RESEARCH.md
@.planning/phases/03-parity-verification/03-CONTEXT.md
</execution_context>

<context>
@debian-bullseye/Dockerfile
@almalinux-8/Dockerfile
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create C++ fixture (clang + mold proof)</name>
  <files>almalinux-8/fixture/cpp/main.cpp</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\fixture\cpp\main.cpp (the file being created)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-RESEARCH.md ("Code Examples" -> fixture/cpp/main.cpp)
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (mold shim, lines 38-62; parity source of truth)
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (mold shim, lines 112-124)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Create `almalinux-8/fixture/cpp/main.cpp` matching the validated example in 03-RESEARCH.md ("Code Examples" -> fixture/cpp/main.cpp). It includes `<iostream>`, `<numeric>`, `<string>`, `<vector>`; builds a `std::vector<int>{1,2,3,4,5}`; computes `std::accumulate(v.begin(), v.end(), 0)`; and prints exactly `cpp-ok 15` followed by a newline. Do NOT embed any version string or compiler macro in the output (Pitfall 2 / D-06). Do NOT place any compiler flags in the source — the harness compiles it with plain `clang++ -std=c++17 -O2` and relies on the update-alternatives ld shim to select mold (Pitfall 1 / Pattern 4); the source must contain no `-fuse-ld=mold` reference anywhere, even in a comment.
  </action>
  <verify>
    <automated>test -s almalinux-8/fixture/cpp/main.cpp && grep -q 'cpp-ok' almalinux-8/fixture/cpp/main.cpp && grep -q 'accumulate' almalinux-8/fixture/cpp/main.cpp && ! grep -q 'fuse-ld' almalinux-8/fixture/cpp/main.cpp</automated>
  </verify>
  <acceptance_criteria>
    - File `almalinux-8/fixture/cpp/main.cpp` exists and is non-empty.
    - It contains the literal output string `cpp-ok` (run output will be `cpp-ok 15`).
    - It contains `std::accumulate` and `std::vector`.
    - It does NOT contain the substring `fuse-ld`.
    - It does NOT print any version string (no `__VERSION__`, no `clang --version` output path).
  </acceptance_criteria>
  <done>File exists and is a deterministic, fixed-string C++ program producing `cpp-ok 15` with no embedded version and no `-fuse-ld` usage.</done>
</task>

<task type="auto">
  <name>Task 2: Create Rust fixture (std-only crate)</name>
  <files>almalinux-8/fixture/rust/Cargo.toml, almalinux-8/fixture/rust/src/main.rs</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\fixture\rust\Cargo.toml (the file being created)
    - W:\gnus\CIDocker\almalinux-8\fixture\rust\src\main.rs (the file being created)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-RESEARCH.md ("Code Examples" -> fixture/rust/Cargo.toml and src/main.rs)
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (RUST_VERSION=1.87.0, ENV block lines 1-7)
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (rustup block, lines 156-172)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Create `almalinux-8/fixture/rust/Cargo.toml` with a `[package]` section: name `parity-fixture`, version `0.1.0`, edition `2021`, and NO `[dependencies]` section (std-only, so `cargo run` performs no crates.io fetch — RESEARCH.md "Don't Hand-Roll" table). Create `almalinux-8/fixture/rust/src/main.rs` with a `main` function that builds `vec![1u64,2,3,4,5]`, sums the iterator with `.iter().sum()`, and prints exactly `rust-ok 15` via `println!("rust-ok {total}")` where `total` is the computed `u64` sum. Do NOT print `rustc --version` or any version string (Pitfall 2 / D-06).
  </action>
  <verify>
    <automated>test -s almalinux-8/fixture/rust/Cargo.toml && test -s almalinux-8/fixture/rust/src/main.rs && grep -q 'rust-ok' almalinux-8/fixture/rust/src/main.rs && ! grep -q '\[dependencies\]' almalinux-8/fixture/rust/Cargo.toml && grep -q 'edition' almalinux-8/fixture/rust/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Files `almalinux-8/fixture/rust/Cargo.toml` and `almalinux-8/fixture/rust/src/main.rs` exist and are non-empty.
    - `Cargo.toml` contains a `[package]` section with `edition` set to `2021`.
    - `Cargo.toml` does NOT contain a `[dependencies]` section.
    - `src/main.rs` contains the literal `rust-ok` output string and an iterator sum over a `u64` vector.
    - `src/main.rs` prints no version string.
  </acceptance_criteria>
  <done>A std-only Rust crate exists that compiles with rustc 1.87.0 on both images and prints the fixed string `rust-ok 15`.</done>
</task>

<task type="auto">
  <name>Task 3: Create GTK compile probe (display-free)</name>
  <files>almalinux-8/fixture/gtk_probe.c</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\fixture\gtk_probe.c (the file being created)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-RESEARCH.md ("Code Examples" -> fixture/gtk_probe.c; "GTK Gap Check")
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (in-image probe convention `pkg-config --exists gtk+-3.0`, line 96)
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (libgtk-3-dev package, line 30)
    - W:\gnus\CIDocker\.planning\phases\03-parity-verification\03-CONTEXT.md (D-07)
  </read_first>
  <action>
    Create `almalinux-8/fixture/gtk_probe.c` matching the validated example in 03-RESEARCH.md ("Code Examples" -> fixture/gtk_probe.c): it contains `#include <gtk/gtk.h>` and a `main` that returns `gtk_get_major_version() == 3 ? 0 : 1`. This is the display-free accessor — it proves gtk3-devel headers/libs resolve at compile+link time without requiring an X/Wayland display. Do NOT call `gtk_init` (Pitfall 5); the harness compiles it with `cc gtk_probe.c -o /tmp/gtk_probe $(pkg-config --cflags --libs gtk+-3.0)` and runs it.
  </action>
  <verify>
    <automated>test -s almalinux-8/fixture/gtk_probe.c && grep -q '#include <gtk/gtk.h>' almalinux-8/fixture/gtk_probe.c && grep -q 'gtk_get_major_version' almalinux-8/fixture/gtk_probe.c && ! grep -q 'gtk_init' almalinux-8/fixture/gtk_probe.c</automated>
  </verify>
  <acceptance_criteria>
    - File `almalinux-8/fixture/gtk_probe.c` exists and is non-empty.
    - It contains `#include <gtk/gtk.h>`.
    - It contains `gtk_get_major_version`.
    - It does NOT contain `gtk_init`.
  </acceptance_criteria>
  <done>A display-free GTK compile probe exists that proves EL8 `gtk3-devel` headers/libs resolve and that both images link gtk+-3.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> fixture source | Fixture source is in-repo (trusted); only the parity harness compiles it, never external users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | fixture source files | accept | In-repo, committed and reviewed; no untrusted input reaches the fixture. |
| T-03-02 | Information Disclosure | fixture build/run | mitigate | Fixture prints only fixed strings (`cpp-ok 15`, `rust-ok 15`); no env vars, secrets, or network calls. Rust crate is std-only (no crates.io fetch). |
| T-03-SC | Tampering | package installs | accept | No npm/pip/crates/RPM packages are installed this phase; Package Legitimacy Audit is N/A (RESEARCH.md). |
</threat_model>

<verification>
- All four fixture files exist and are non-empty: `test -s` on each path.
- Content gates pass for each file (see each task's `<verify>`).
- No fixture file contains `-fuse-ld=mold`, an embedded version string, a `[dependencies]` section, or `gtk_init`.
</verification>

<success_criteria>
- `almalinux-8/fixture/` contains `cpp/main.cpp`, `rust/Cargo.toml`, `rust/src/main.rs`, and `gtk_probe.c`.
- The C++ and Rust fixtures emit fixed strings (`cpp-ok 15`, `rust-ok 15`) so both images' stdout can be `diff`ed (D-06).
- The C++ fixture compiles with plain `clang++` (mold via the ld shim), keeping bullseye clang 11 compatible (Pattern 4).
- The GTK probe is compile/run-safe without a display, satisfying the PAR-02 header/lib resolution bar (D-07).
</success_criteria>

<output>
Create `.planning/phases/03-parity-verification/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| File | Symbol(s) | Purpose |
|------|-----------|---------|
| `almalinux-8/fixture/cpp/main.cpp` | `int main()` | clang + mold behavioral build proof (PAR-01) |
| `almalinux-8/fixture/rust/Cargo.toml` | package `parity-fixture` | std-only crate manifest |
| `almalinux-8/fixture/rust/src/main.rs` | `fn main()` | Rust behavioral build proof (PAR-01) |
| `almalinux-8/fixture/gtk_probe.c` | `int main()` | GTK header/lib resolution probe (PAR-02) |
