---
phase: 01-base-package-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [almalinux-8/Dockerfile]
autonomous: true
requirements: [BASE-01, BASE-02]

must_haves:
  truths:
    - "Image is based on almalinux:8 and reports glibc 2.28 via ldd --version"
    - "All five ordered repos are enabled in the required sequence (dnf-plugins-core bootstrap → epel-release → powertools → gh-cli → NodeSource) plus the llvm-toolset module"
    - "docker build succeeds and the walking-skeleton image runs"
  artifacts:
    - path: "almalinux-8/Dockerfile"
      provides: "Base image + ENV contract + ordered repo enablement + walking-skeleton package slice"
      min_lines: 40
      contains: "FROM almalinux:8"
    - path: ".planning/phases/01-base-package-install/SKELETON.md"
      provides: "Walking-skeleton architectural contract for subsequent phases"
  key_links:
    - from: "almalinux-8/Dockerfile"
      to: "registry almalinux:8"
      via: "FROM instruction"
      pattern: "^FROM almalinux:8"
    - from: "almalinux-8/Dockerfile"
      to: "dnf config-manager"
      via: "dnf-plugins-core bootstrap layer"
      pattern: "dnf install -y dnf-plugins-core"
    - from: "almalinux-8/Dockerfile"
      to: "built image"
      via: "docker build + docker run ldd --version"
      pattern: "ldd \\(GNU libc\\) 2\\.28"
---

<objective>
Create the walking-skeleton `almalinux-8/Dockerfile` and the `SKELETON.md` architectural contract.

**User story:** **As a** CI engineer, **I want to** build an AlmaLinux 8-based image whose base OS, environment contract, and ordered dnf repositories are in place, **so that** a buildable, runnable image exists that reports glibc 2.28 and proves the repo-enablement pipeline works end-to-end.

Purpose: Establish the thinnest buildable state of the image — `FROM almalinux:8` (glibc 2.28), the ENV contract carried from `debian-bullseye`, all six ordered repo/module enablement steps, and a minimal package slice that proves `dnf install` works across the enabled repos. The full PKG-01 package set and in-image verification land in Plan 02.

Output: `almalinux-8/Dockerfile` (walking skeleton) + `.planning/phases/01-base-package-install/SKELETON.md`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-base-package-install/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/phases/01-base-package-install/01-PATTERNS.md
@debian-bullseye/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create walking-skeleton almalinux-8/Dockerfile (base + ENV + ordered repos + thin package slice)</name>
  <files>almalinux-8/Dockerfile</files>
  <read_first>
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (the exact analog — heredoc + set -eux style, ENV contract)
    - W:\gnus\CIDocker\.planning\phases\01-base-package-install\01-RESEARCH.md (authoritative ordered dnf recipe)
    - W:\gnus\CIDocker\.planning\research\STACK.md (Debian→EL8 package mapping + repo rationale)
    - W:\gnus\CIDocker\.planning\phases\01-base-package-install\01-PATTERNS.md (heredoc/continuation style conventions)
  </read_first>
  <action>
    Create the NEW file `almalinux-8/Dockerfile` (directory `almalinux-8/` does not exist yet — create it). This is a walking skeleton; the full PKG-01 package list is Plan 02's job, so keep this slice minimal but REAL (it must prove the repo-enablement + package-install pipeline works end-to-end).

    Structure, in exact order:

    1. Line 1: `# syntax=docker/dockerfile:1`; line 2 (immediately, no comments/ARG between): `FROM almalinux:8`.

    2. ENV contract — copy the multi-line ENV from `debian-bullseye/Dockerfile` lines 4-7 verbatim (RUSTUP_HOME, CARGO_HOME, PATH, RUST_VERSION=1.87.0). Keep the `...:/bin:$PATH` trailing `$PATH` append form exactly as in the analog (do NOT hardcode the literal PATH from the RESEARCH recipe — the analog's append form is authoritative).

    3. Repo/module enablement as SIX ordered `RUN` layers, each on its own line, using the heredoc (`RUN <<EOF` + blank line + `set -eux` + blank line + body + blank line + `EOF`) convention from the analog for any multi-command layer, and plain `RUN` single-command form where the recipe is one command:
       - Step 1 (bootstrap): `dnf install -y dnf-plugins-core wget curl tar gzip xz ca-certificates chkconfig && dnf clean all` — `dnf-plugins-core` MUST precede every `dnf config-manager` call. Wrap with `set -eux`.
       - Step 2: `dnf install -y epel-release && dnf clean all` (pulled from Extras, which almalinux:8 ships enabled).
       - Step 3: `dnf config-manager --set-enabled powertools` (AlmaLinux 8 repo id is `powertools` — NEVER `crb`, that is AlmaLinux 9).
       - Step 4: `dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo`.
       - Step 5: `curl -fsSL https://rpm.nodesource.com/setup_24.x -o /tmp/nodesource_setup.sh && bash /tmp/nodesource_setup.sh && rm /tmp/nodesource_setup.sh && dnf clean all` — fetch via HTTPS, fail on error, delete script after use. `nodejs` itself is NOT installed in Phase 1.
       - Step 6: `dnf module enable llvm-toolset -y` (clang is delivered ONLY via this AppStream module).

    4. Thin walking-skeleton package slice as ONE heredoc `RUN` layer with `set -eux`, installing only: `pkgconf-pkg-config sudo git wget curl jq`, then `dnf clean all && rm -rf /var/cache/dnf` in the SAME layer. Mark this block with a comment line `# [walking-skeleton package slice — Plan 02 replaces this with the full PKG-01 list]` so Plan 02 can locate it unambiguously.

    Hard requirements: `gpgcheck=1` everywhere — the file must never contain `--nogpgcheck` or `gpgcheck=0`. `dnf clean all` must live in the same RUN layer as each `dnf install`. Do not add an unconditional `dnf upgrade -y`. Do not remove `/var/lib/dnf` or repo metadata (Phase 2 needs dnf again). Do not add any mold/Node/Rust/JDK layers (Phase 2). Do not add arch branching (Phase 2+). Use lowercase directory `almalinux-8` mirroring `debian-bullseye`.
  </action>
  <verify>
    <automated>docker info &gt;/dev/null 2&gt;&amp;1 &amp;&amp; docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .</automated>
  </verify>
  <acceptance_criteria>
    - `almalinux-8/Dockerfile` exists; line 1 is `# syntax=docker/dockerfile:1` and line 2 is `FROM almalinux:8` (no comment/ARG between them).
    - The file contains, in this order: `dnf install -y dnf-plugins-core` (step 1) before `dnf config-manager --set-enabled powertools` (step 3) and `dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo` (step 4).
    - The file contains all six steps verbatim: `epel-release`, `powertools`, `gh-cli.repo`, `rpm.nodesource.com/setup_24.x`, `dnf module enable llvm-toolset -y`.
    - The file contains `dnf clean all` in the same RUN layer as each `dnf install`.
    - The file does NOT contain `--nogpgcheck`, `gpgcheck=0`, or `crb`.
    - `docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .` exits 0.
    - `docker run --rm cidocker-almalinux-8:phase1 ldd --version | head -1` output contains `2.28`.
    - `docker run --rm cidocker-almalinux-8:phase1 dnf repolist` lists baseos, appstream, extras, epel, powertools, gh-cli, and a nodesource repo.
    - If the Docker daemon is not running, the executor records the blocker in the SUMMARY and marks the Docker-dependent acceptance criteria as blocked (daemon start is a human action).
  </acceptance_criteria>
  <done>almalinux-8/Dockerfile builds successfully, reports glibc 2.28, and has all six ordered repo/module enablement steps plus a working thin package slice.</done>
</task>

<task type="auto">
  <name>Task 2: Write SKELETON.md walking-skeleton contract</name>
  <files>.planning/phases/01-base-package-install/SKELETON.md</files>
  <read_first>
    - W:\gnus\CIDocker\.planning\phases\01-base-package-install\01-RESEARCH.md (architectural decisions: base image, repo order, compiler)
    - C:\Users\Fuu\.claude\gsd-core\references\skeleton-template.md (template structure)
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (env contract + toolchain pins referenced as deferred)
  </read_first>
  <action>
    Create `.planning/phases/01-base-package-install/SKELETON.md` following the skeleton template structure, translated to this Docker base-image project:

    - **Capability Proven End-to-End** (one sentence): `almalinux-8/Dockerfile` builds with `docker build` and runs; `ldd --version` reports glibc 2.28 and `dnf repolist` shows the ordered enabled repos (baseos, appstream, extras, epel, powertools, gh-cli, nodesource) — image tag `cidocker-almalinux-8:phase1`.
    - **Architectural Decisions** table rows exactly: Base image → `almalinux:8` (rolling, glibc 2.28, amd64+arm64); Package manager → `dnf`; Repo enablement order → dnf-plugins-core → epel-release → powertools → gh-cli.repo → NodeSource setup_24.x, then `dnf module enable llvm-toolset`; Compiler → clang via llvm-toolset module (gcc/gcc-c++ kept only for cc + libc headers); Directory layout → `almalinux-8/Dockerfile` alongside `debian-bullseye/Dockerfile` (CI keys off directory name).
    - **Stack Touched in Phase 1** checklist: base image (`FROM almalinux:8`), repo enablement, package install (walking-skeleton slice in Plan 01, full PKG-01 list in Plan 02), cache cleanup (`dnf clean all` + `rm -rf /var/cache/dnf`), build-time verification (`ldd --version` → 2.28, `dnf repolist`).
    - **Out of Scope (Deferred)** list: mold/Node/Rust/JDK toolchain (Phase 2), parity verification vs debian-bullseye + GTK gap audit (Phase 3), multi-arch buildx sign-off (Phase 4), full PKG-01 package list completion (Plan 02 of this phase).
    - **Subsequent Slice Plan**: Phase 2 → toolchain install (mold 2.42.0, Node 24, Rust 1.87.0, Temurin JDK 25); Phase 3 → parity & verification; Phase 4 → multi-arch verification.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Get-Content '.planning/phases/01-base-package-install/SKELETON.md' | Select-String 'Capability Proven End-to-End','Architectural Decisions','Stack Touched in Phase 1','Out of Scope','Subsequent Slice Plan'"</automated>
  </verify>
  <acceptance_criteria>
    - `SKELETON.md` exists at `.planning/phases/01-base-package-install/SKELETON.md`.
    - Contains the section headers: `Capability Proven End-to-End`, `Architectural Decisions`, `Stack Touched in Phase 1`, `Out of Scope (Deferred to Later Slices)`, `Subsequent Slice Plan`.
    - Architectural Decisions mentions `almalinux:8`, `dnf`, `powertools`, `gh-cli.repo`, `rpm.nodesource.com/setup_24.x`, `llvm-toolset`, and `almalinux-8/Dockerfile`.
    - Out of Scope lists mold/Node/Rust/JDK → Phase 2, parity → Phase 3, multi-arch → Phase 4.
  </acceptance_criteria>
  <done>SKELETON.md records the architectural decisions subsequent phases build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build → rpm.nodesource.com | Untrusted remote script fetched and executed during build |
| Docker build → cli.github.com | Third-party vendor repo added to the image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | NodeSource setup_24.x script fetch | mitigate | `curl -fsSL` over TLS; delete `/tmp/nodesource_setup.sh` after use; never `--nogpgcheck` |
| T-01-02 | Tampering | gh-cli.repo + epel-release packages | mitigate | `gpgcheck=1` on all repos; official repo files only; never `gpgcheck=0` |
| T-01-03 | Elevation of privilege | Base image provenance | mitigate | Official `almalinux:8` Docker Official Image only; no unofficial mirrors |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No npm/PyPI/crates packages in this phase — all packages are official RPMs; slopcheck not applicable (see 01-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .  # exits 0
docker run --rm cidocker-almalinux-8:phase1 ldd --version | head -1      # contains 2.28
docker run --rm cidocker-almalinux-8:phase1 dnf repolist                 # baseos appstream extras epel powertools gh-cli nodesource
</verification>

<success_criteria>
1. `almalinux-8/Dockerfile` starts with `FROM almalinux:8` and builds cleanly.
2. The built image reports glibc 2.28 via `ldd --version`.
3. All six ordered repo/module enablement steps are present and `dnf repolist` lists the enabled repos.
4. `SKELETON.md` exists with the five required sections and correct decisions.
</success_criteria>

<output>
Create `.planning/phases/01-base-package-install/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `almalinux-8/Dockerfile` — NEW file (walking skeleton in this plan; full PKG-01 package list added in Plan 02)
- `.planning/phases/01-base-package-install/SKELETON.md` — planning artifact (walking-skeleton contract)
- `.planning/phases/01-base-package-install/01-01-SUMMARY.md` — execution summary (generated at execution time)
