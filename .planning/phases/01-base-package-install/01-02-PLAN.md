---
phase: 01-base-package-install
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [almalinux-8/Dockerfile]
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "Every EL8 package in PKG-01 is installed in one dnf transaction"
    - "The full package set is verifiable in the built image (pkg-config --exists for vulkan/gtk3/libsecret, version commands for clang/cmake/ninja/gh/ruby/jq)"
    - "docker build completes with dnf clean all leaving no package cache"
  artifacts:
    - path: "almalinux-8/Dockerfile"
      provides: "Full PKG-01 package list + in-image verification chain"
      contains: "gtk3-devel"
  key_links:
    - from: "almalinux-8/Dockerfile"
      to: "installed packages"
      via: "single dnf install -y transaction"
      pattern: "dnf install -y .*gtk3-devel"
    - from: "almalinux-8/Dockerfile"
      to: "pkg-config metadata"
      via: "in-image verification chain"
      pattern: "pkg-config --exists vulkan"
---

<objective>
Complete the walking skeleton into the final Phase 1 image: replace the thin package slice with the FULL PKG-01 EL8 package set (one dnf transaction), append an in-image build-time verification chain, and prove the build finishes clean with no dnf package cache.

Purpose: Fulfill PKG-01 — every bullseye system package mapped to its EL8 equivalent, installed atomically, verified inside the image at build time, and left cache-clean.

Output: `almalinux-8/Dockerfile` finalized for Phase 1 (package-install layer complete + verification layer appended)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-base-package-install/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/phases/01-base-package-install/01-PATTERNS.md
@debian-bullseye/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace walking-skeleton slice with full PKG-01 package list (single dnf transaction)</name>
  <files>almalinux-8/Dockerfile</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (the file being edited — locate the `[walking-skeleton package slice — Plan 02 replaces this...]` comment block)
    - W:\gnus\CIDocker\.planning\phases\01-base-package-install\01-RESEARCH.md (step 7 full package list + cache-cleanup strategy)
    - W:\gnus\CIDocker\.planning\research\STACK.md (package mapping + repo rationale)
    - W:\gnus\CIDocker\debian-bullseye\Dockerfile (analog single-apt-install block being re-mapped)
  </read_first>
  <action>
    Edit `almalinux-8/Dockerfile`: find the thin package slice (the `RUN` block preceded by the comment `# [walking-skeleton package slice — Plan 02 replaces this with the full PKG-01 list]`) and replace it with ONE heredoc `RUN` layer (`RUN <<EOF` + blank + `set -eux` + blank + body + blank + `EOF`).

    The body installs the FULL mapped package set in ONE `dnf install -y` transaction, one package per line, 4-space indent, ` \` continuation on every line except the last, ending with `&& dnf clean all \` then `&& rm -rf /var/cache/dnf` on the final continuation:

        dnf install -y \
            sudo \
            pkgconf-pkg-config \
            git \
            ruby ruby-devel \
            clang \
            cmake \
            make gcc gcc-c++ binutils \
            gh \
            openssl-devel \
            libcurl-devel \
            libsecret-devel \
            dbus dbus-daemon dbus-tools \
            gnome-keyring \
            ninja-build \
            vulkan-loader vulkan-loader-devel vulkan-headers \
            gtk3-devel \
            jq \
            libatomic \
        && dnf clean all \
        && rm -rf /var/cache/dnf

    Notes: `nodejs` is deliberately EXCLUDED (Phase 2). `chkconfig` is already in the bootstrap layer (Plan 01); do not duplicate it. `dnf clean all` and `rm -rf /var/cache/dnf` MUST stay in the SAME layer (Docker layers are immutable). Do NOT remove `/var/lib/dnf` or repo metadata. Do NOT add `dnf upgrade -y`. Keep the install layer as the LAST dnf layer so repo setup stays cacheable. Use the heredoc + `set -eux` form (not plain `RUN ... \` continuations) to match the analog exactly.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'almalinux-8/Dockerfile' -Pattern 'pkgconf-pkg-config','gtk3-devel','vulkan-headers','libcurl-devel','libsecret-devel','ninja-build','gnome-keyring','ruby ruby-devel','dbus dbus-daemon dbus-tools','make gcc gcc-c++ binutils','openssl-devel','libatomic' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - The `[walking-skeleton package slice...]` comment and its thin package list are GONE; the install layer now contains every package below, each present verbatim:
      `sudo`, `pkgconf-pkg-config`, `git`, `ruby ruby-devel`, `clang`, `cmake`, `make gcc gcc-c++ binutils`, `gh`, `openssl-devel`, `libcurl-devel`, `libsecret-devel`, `dbus dbus-daemon dbus-tools`, `gnome-keyring`, `ninja-build`, `vulkan-loader vulkan-loader-devel vulkan-headers`, `gtk3-devel`, `jq`, `libatomic`.
    - The install layer contains `&& dnf clean all` and `&& rm -rf /var/cache/dnf` in the SAME RUN block.
    - `nodejs` does NOT appear in the file.
    - The file still contains `FROM almalinux:8` and the six repo/module enablement steps from Plan 01.
    - The file contains no `--nogpgcheck` / `gpgcheck=0` / `crb`.
  </acceptance_criteria>
  <done>almalinux-8/Dockerfile installs the full PKG-01 package set in one dnf transaction with same-layer cache cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Append in-image verification chain and prove clean build</name>
  <files>almalinux-8/Dockerfile</files>
  <read_first>
    - W:\gnus\CIDocker\almalinux-8\Dockerfile (the file being edited — append after the install layer)
    - W:\gnus\CIDocker\.planning\phases\01-base-package-install\01-RESEARCH.md ("Verifying PKG-01" chain + Build-Time Validation Checklist items 1-3, 5, 9)
  </read_first>
  <action>
    Append ONE final heredoc `RUN` layer to `almalinux-8/Dockerfile` (after the package-install layer) that fails the build if any package is missing or unresolved. Use `RUN <<EOF` + `set -eux` form.

    Body runs, in order (each must succeed or the build fails):

        pkg-config --version && gcc --version | head -1 && clang --version | head -1 \
          && cmake --version | head -1 && ninja --version && git --version \
          && gh --version && ruby --version && wget --version | head -1 \
          && curl --version | head -1 && jq --version && ld --version | head -1 \
          && pkg-config --exists vulkan && pkg-config --exists gtk+-3.0 \
          && pkg-config --exists libsecret-1 && rpm -q libatomic

    Then add the three build-time probes for the unresolved repo placements from the research checklist, run as non-fatal diagnostics (report only, so they inform but do not hard-fail):
        `dnf info libsecret-devel | grep -E '^Repo' || true`; `dnf info libcurl-devel | grep -E '^Repo' || true`; `dnf info vulkan-loader-devel | grep -E '^Repo' || true`.

    Keep `gpgcheck=1`; do not add `dnf clean all` to this layer (it is verification-only). Do not add mold/Node/Rust/JDK content (Phase 2).
  </action>
  <verify>
    <automated>docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .</automated>
  </verify>
  <acceptance_criteria>
    - `almalinux-8/Dockerfile` contains a final verification RUN layer with `pkg-config --exists vulkan`, `pkg-config --exists gtk+-3.0`, `pkg-config --exists libsecret-1`, and `rpm -q libatomic`.
    - `docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .` exits 0.
    - Build log shows the verification chain ran with no failing command (no `Command exited with non-zero` in the verification layer).
    - `docker run --rm cidocker-almalinux-8:phase1 pkg-config --exists vulkan && docker run --rm cidocker-almalinux-8:phase1 pkg-config --exists gtk+-3.0` exits 0.
    - `docker images cidocker-almalinux-8:phase1 --format "{{.Size}}"` reports a size (no dnf package cache left behind — the `dnf clean all` + `rm -rf /var/cache/dnf` from Task 1 took effect).
    - If the Docker daemon is not running, the executor records the blocker in the SUMMARY and marks the Docker-dependent acceptance criteria as blocked.
  </acceptance_criteria>
  <done>almalinux-8/Dockerfile builds clean, every PKG-01 package verifies in-image, and no dnf package cache remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build → cli.github.com / rpm.nodesource.com | Third-party vendor packages installed into the image |
| Docker build → repo.almalinux.org | Distro packages resolved via module streams (llvm-toolset, ruby) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | gh-cli.repo / NodeSource packages | mitigate | `gpgcheck=1` on all repos; official repo files only; never `--nogpgcheck` |
| T-01-05 | Tampering | llvm-toolset module stream (clang) | mitigate | enable module explicitly; verification chain asserts `clang --version` at build time |
| T-01-06 | Elevation of privilege | excess packages / attack surface | mitigate | install ONLY the parity package set — no X server, desktop, docs, or extras; `dnf clean all` + `rm -rf /var/cache/dnf` |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No npm/PyPI/crates packages — all packages are official RPMs; slopcheck not applicable |
</threat_model>

<verification>
docker build -t cidocker-almalinux-8:phase1 -f almalinux-8/Dockerfile .          # exits 0
docker run --rm cidocker-almalinux-8:phase1 ldd --version | head -1               # 2.28
docker run --rm cidocker-almalinux-8:phase1 pkg-config --exists vulkan            # exits 0
docker run --rm cidocker-almalinux-8:phase1 pkg-config --exists gtk+-3.0          # exits 0
docker run --rm cidocker-almalinux-8:phase1 clang --version | head -1             # 17.x via llvm-toolset
</verification>

<success_criteria>
1. The full PKG-01 package list installs in one dnf transaction.
2. `docker build` completes with `dnf clean all` and no dnf package cache (lean image).
3. The in-image verification chain passes (vulkan/gtk3/libsecret pkg-config checks + version commands).
4. Image reports glibc 2.28 and all six repos enabled (carried from Plan 01).
</success_criteria>

<output>
Create `.planning/phases/01-base-package-install/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `almalinux-8/Dockerfile` — NEW file, finalized in this plan: base + ENV + ordered repos (Plan 01) + full PKG-01 package list + in-image verification chain (Plan 02)
- `.planning/phases/01-base-package-install/SKELETON.md` — planning artifact (created in Plan 01)
- `.planning/phases/01-base-package-install/01-02-SUMMARY.md` — execution summary (generated at execution time)
